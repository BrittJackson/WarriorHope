/**
 * This module contains the limits for group sizes in one convienent place.
 * This allows other modules to use the same values in case something changes.
 * @exports groupLimits 
*/
module.exports = {
    MIN: 5,
    ADMIN: 12,
    MAX: 16,
}